{
  "name": "electronic-prescription-service-api-models",
  "version": "0.0.1",
  "description": "Electronic Prescription Service API Models",
  "scripts": {
    "build": "tsc --build --clean ./tsconfig.json",
    "lint": "eslint . --ext .ts --max-warnings 0 --fix",
    "test": "jest --ci --reporters=default --reporters=jest-junit --coverage",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../.."
  },
  "author": "NHS Digital",
  "license": "MIT",
  "engines": {
    "node": "16.14.x",
    "npm": "8.x"
  },
  "dependencies": {
    "axios": "^1.3.6",
    "fhirpath": "^3.3.1",
    "lossless-json": "^2.0.5",
    "xml-js": "^1.6.11"
  },
  "devDependencies": {
    "@types/fhir": "^0.0.36",
    "@types/lossless-json": "^1.0.1"
  }
}

components:
  schemas:
    Telecom:
      type: object
      required:
        - system
        - use
        - value
      properties:
        system:
          description: Mean of communication allowed is Phone only
          type: string
          enum: [phone]
          default: phone
        use:
          type: string
          description: Location associated with the contact details provided.
          enum: [home, work, temp, mobile]
          default: work
        value:
          description: Phone number for use with contact system.
          type: string
          example: '01234567890'

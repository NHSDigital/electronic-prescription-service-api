components:
  schemas:
    DispenseNotification:
      type: object
      required: 
        - resourceType
        - entry
        - type
      properties:
        resourceType: 
          type: string
          description: FHIR resource type.
          enum : [Bundle]
        type: 
          type: string
          emun: [message]
        entry:
          type: array
          items: 
            allOff:
              - $ref: link out MessageHeader
              - $ref: link out MedicationDispense
              - $ref: '../../components/Organization/ReleaseOrganization.yaml'

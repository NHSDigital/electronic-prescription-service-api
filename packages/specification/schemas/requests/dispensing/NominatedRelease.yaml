type: object
properties:
  resourceType:
    type: string
    description: FHIR resource type.
    enum: [Parameters]
  
  parameter:
    type: array
    items:
      oneOf:
      - $ref: './_status.yaml'
      - $ref: './_agent.yaml'
      - $ref: './_owner.yaml'

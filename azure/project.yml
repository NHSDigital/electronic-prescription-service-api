variables:
  service_name: electronic-prescription-service-api
  short_service_name: eps-coordinator
  service_base_path: electronic-prescriptions
  product_display_name: Electronic Prescription Service
  product_description: Placeholder description
  service_base_path_pr: ${{ variables.service_base_path }}-pr-${{ replace(replace(replace(variables['Build.SourceBranch'], '/merge', ''), 'refs/pull/', ''), '/', '_') }}
